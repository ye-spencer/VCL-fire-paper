# Fireplace Slider Data Analysis - Fire vs Pretty-Danger

## What it does

Creates a histogram of the slope of the relationship between "pretty/danger" and "like fire" using each chunk as a data point. Each data point in the histogram is a single participant's slope.